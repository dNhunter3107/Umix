//- extends layout

//- block content
//-   //- - random generate title
//-   p Welcome to #{title}
//-   img(src=imageUrl, alt='Random Image')

extends layout

block content
  .container
    h1(style='text-align: center;')= title
    img(src='logos/logo.png', alt='Logo', style='display: block; margin: 0 auto; height: 100px')
    img(src=imageUrl, alt='Random Image', style='display: block; margin: 0 auto;')
    h1(style='text-align: center;')= randomC
